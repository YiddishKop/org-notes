import urllib2
url = 'https://people.eecs.berkeley.edu/~jrs/61b/lec/'
urlArr = []
for i in range(40):
    if i < 10:
        urlArr.append(url + '0' + str(i+1))
    else: urlArr.append(url + str(i))
print urlArr
# for i in range(len(urlArr)):
#     response = urllib2.urlopen(urlArr[i])
#     webcontent = response.read()
#     output = '** lec ' + str(i + 1) + '\n' + webcontent
    # print output

